events {}

http {
    upstream backend {
        server 127.0.0.1:5000;
        server 127.0.0.1:5001;
    }
    server {
        listen 8082;
        location /health {
            proxy_pass http://backend;
        }
    }
}